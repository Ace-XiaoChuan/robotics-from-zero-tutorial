# 仓库协作约定

本文行数：47行，预期阅读时长：4分钟

## 仓库定位

这是用户的个人机器人工程学习笔记与实践仓库。主要目标是让用户日后快速复习、找回印象，必要时查阅原理和实现。使用中文说明，保留必要的英文术语、公式和 API 名称。

当前主线是机械臂的建模、感知、规划、控制与仿真；数据采集和模仿学习属于后续扩展。学习安排以 [学习路线](doc/learning-roadmap.md) 为准，lesson 编号只是已有案例的稳定标识，不代表知识的先修顺序。

## 用户的持续授权

用户于 2026-09-16 明确表示：

> 另外所有的工作中代码你可以随意重写，一切交给你放手来做，这句话你可以建立一个agents.md保存为记忆。

在本仓库学习整理、实现和修复任务范围内，用户授权自主调整目录、增删改文档、重构或重写代码、修改构建配置并完成必要验证。不要因为“原来是用户手写的”“旧文档写了冻结”“涉及代码改写”而反复询问许可。应根据学习目标判断改动是否必要，直接完成合理范围内的工作。

该授权不表示每次都要重写全部代码，也不等同于已验证所有示例。外部发布、共享、真机操作等仍需遵循其适用授权边界。本文件使用标准名称 `AGENTS.md`，供后续仓库协作读取。

## 文档与目录规则

- 所有 Markdown 文档在主标题下方添加 `本文行数：xx行，预期阅读时长：xx分钟`；没有主标题时放在文首。行数包含空行、代码和说明行本身。新增或修改文档后，从仓库根目录运行 `python3 scripts/update_doc_metadata.py` 刷新统计，并用 `--check` 检查。阅读时长按中文字符每分钟 400 字、英文及数字词每分钟 200 词粗估，向上取整且至少 1 分钟，不包含动手实践时间。
- 用户最新偏好：默认采用“速记要点 + 例子 + 结果与解释”，读者无需执行代码即可完成复习。取消必做实验和练习；只有静态例子难以说明的现象才增加可选实验，并说明它解决什么疑问。
- 通用工程基础保持简洁，AI 可协助处理语法、命令、配置和常规代码；相关细节按需速查。代码与必要的验证材料可保留作参考，agent 对实现正确性的验证不转嫁为读者的学习作业。
- Git 日常操作由 agent 协助处理；相关教学只需帮助用户理解改动范围、提交与推送状态、检查点及恢复意图，不要求背命令或系统学习分支合并。完成实际任务时如实报告验证、提交和推送状态。
- 根据知识价值分配解释深度：机器人核心概念（坐标系、单位、运动学、控制、感知与误差等）仍按理解需要展开；不要把所有工具细节都列为必修，不要求先学完完整工具课程才能推进机器人实践。

- 根目录 README 提供定位和导航；`doc/learning-roadmap.md` 维护学习顺序、先修关系、验收与扩展方向；`src/README.md` 维护案例的主题归属。
- 已有 `src/lessonN/` 作为稳定案例位置。新实验可按路线中的主题采用描述性目录名；只有出现实际内容时才创建目录，不预建大量空课。
- 为改善组织可以迁移、拆分或合并旧案例；迁移时同步更新相对链接、命令、模型路径、导入和构建入口。
- 单案例入口和专题笔记参考 [文档样板](doc/lesson-template.md)。不机械填满模板，不复制多份相同知识点。
- 解释顺序优先采用：核心结论 → 具体例子 → 输入输出与结果 → 原因和易错点。必要推导按知识难度补充；例子结果注明是示意、源码推导还是实际测得，不用操作步骤替代解释。
- 数学与机器人数据写清单位、维度、坐标系、变换方向、关节顺序、时间含义和适用条件。
- 区分已有代码、静态推导、实际运行结果、建议实现和待确认内容。不要因文件存在就标记掌握或验收完成，不替用户编造个人学习经历。
- 参考资料尽量使用与实际环境匹配的官方文档和原始教材；版本变化可能影响 API 时先核对资料。

## 代码与验证规则

- 优先选择能解释、能复现的小型实现。理论推导可从简单模型开始，综合实验再使用机械臂模型。
- 优先修复缺依赖、隐式路径和接口不一致等阻碍复现的问题；外部模型与辅助库应有来源、版本和配置方法。
- 本机绝对路径逐步改为显式参数或可配置路径。不要依靠另一个私人项目的偶然环境来证明示例可用。
- 重构前查看工作区差异，理解现有改动；避免用 Git 回退覆盖用户未提交成果。授权重写不等于可以丢失有价值的笔记和实验记录。
- 检查与改动相称：文档检查链接和命令；数学实现验证可解释的小例子；控制实现记录误差、约束与失败行为。
- 直接写入仿真 `qpos` 可用于初始化和运动学检查，不能单凭它宣称完成了执行器驱动的动力学控制验证。
- 实验记录实际配置和结果；依赖、模型或显示环境不足时，说明未验证的部分。
- 构建产物、运行日志、大型数据和临时截图按仓库忽略规则处理；需要复现的配置、少量指标与说明应留在版本记录中。
